export interface userProps {
    id: number
    firstName: string,
    email: string
    image: string
    lastName: string
    address: {
      address: string,
      city: string
    }
    phone: string
  }